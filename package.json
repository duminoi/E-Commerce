{
  "name": "e-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack E-Commerce platform (NestJS + React + PostgreSQL + Redis)",
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run start:dev --prefix back-end\" \"npm run dev --prefix front-end\"",
    "build": "npm run build --prefix back-end && npm run build --prefix front-end",
    "start:prod": "npm run start:prod --prefix back-end",
    "lint": "npm run lint --prefix back-end",
    "seed:admin": "npm run seed:admin --prefix back-end",
    "seed:product": "npm run seed:product --prefix back-end"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/duminoi/E-Commerce.git"
  },
  "keywords": [
    "ecommerce",
    "nestjs",
    "react",
    "postgresql"
  ],
  "author": "",
  "license": "ISC"
}
